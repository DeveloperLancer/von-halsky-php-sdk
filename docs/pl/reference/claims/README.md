# Claims

`types()` jest globalne. Pozostałe metody wymagają `$shop->claims()`.

- [`types()`](./types.md), [`list()`](./list.md), [`get()`](./get.md), [`reject()`](./reject.md), [`partialRefund()`](./partial-refund.md), [`refund()`](./refund.md)
