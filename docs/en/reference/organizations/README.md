# Organizations

Organizations are global to the current token. Select an `OrganizationId` from this resource before using merchant-scoped resources.

- [`list()`](./list.md) — list organizations available to the current token.
